# MongoDB Connection String
MONGODB_URI=mongodb+srv://karthikeya:BlogHub2026@cluster0.qb4ufyz.mongodb.net/bloghub?retryWrites=true&w=majority

# Admin API Key (use for approving blogs/users via /api/admin routes)
ADMIN_API_KEY=your_secret_admin_key_here_123

# Admin Portal Login (Basic Auth for /admin)
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS=admin123

# Serve Static Files (set to true to serve HTML/CSS/JS from backend)
SERVE_STATIC=true

# Frontend Origins (allowed domains for CORS, comma-separated)
FRONTEND_ORIGIN=http://localhost:3000,http://localhost:5500

# Server Port
PORT=3000
