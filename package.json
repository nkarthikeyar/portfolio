{
  "name": "bloghub-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix BACKEND",
    "install": "npm install --prefix BACKEND"
  },
  "engines": {
    "node": "18.x"
  }
}
